#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int n, best;
  cin >> n;
  vector<int> a(n), d(n);
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }
  int k = 3;
  for (int i = 0; i < n; i++) {
  }
}
