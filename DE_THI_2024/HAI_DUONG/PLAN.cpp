#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);
  cout.tie(nullptr);

  long long n, k;
  cin >> n >> k;

  vector<long long> a(n), b(n);

  for (auto x : a) {
    cin >> x;
  }
  for (auto x : b) {
    cin >> x;
  }
}
